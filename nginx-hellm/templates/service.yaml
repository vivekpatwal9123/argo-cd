apiVersion: v1
kind: Service
metadata:
  name: {{ include "nginx-hellm.fullname" . }}
  labels:
    {{- include "nginx-hellm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "nginx-hellm.selectorLabels" . | nindent 4 }}
